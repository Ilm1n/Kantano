# Empty value uses same-origin /api and /ws (Vite proxy in development, Caddy in production).
VITE_API_URL=

# For a separate backend host use its origin without a trailing slash or /api suffix:
# VITE_API_URL=http://localhost:8000
